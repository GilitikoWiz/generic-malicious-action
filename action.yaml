name: 'Setup'
description: 'Sets up the environment for your project'
runs:
  using: 'composite'
  steps:
    - name: Setup environment
      run: node ${{ github.action_path }}/scripts/setup.js
      shell: bash
